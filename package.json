{
  "name": "playground-finding-app",
  "version": "1.0.0",
  "description": "A really cool web application where parents can browse all playgrounds in Germany, and Check-In",
  "scripts": {
    "server-start-prod": "NODE_ENV=production node server.js",
    "server-start-dev": "nodemon server.js",
    "client-start-dev": "cd client && npm start",
    "build": "cd client && npm run build",
    "postinstall": "cd client && npm install"
  },
  "author": "Michail Mangasarov (https://github.com/mikemangas/)",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.12",
    "prettier": "2.3.2"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.6.0",
    "express": "^4.17.1",
    "mongoose": "^5.13.7",
    "nodemailer": "^6.6.5"
  }
}
